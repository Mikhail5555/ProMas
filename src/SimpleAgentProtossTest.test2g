use SimpleAgentProtoss as mas.
use General.GeneralEvents as module.
use Manager.Manager as module.
use ProtossForge.ProtossForge as module.
use ProtossGateway.ProtossGateway as module.
use ProtossNexus.ProtossNexus as module.
use ProtossProbe.ProtossProbe as module.
use ProtossZealot.ProtossZealot as module.
use General.General as knowledge.
use Manager.Manager as knowledge.
use ProtossForge.ProtossForge as knowledge.
use ProtossGateway.ProtossGateway as knowledge.
use ProtossNexus.ProtossNexus as knowledge.
use ProtossProbe.ProtossProbe as knowledge.
use ProtossZealot.ProtossZealot as knowledge.
use ProtossNexus.ProtossNexus as actionspec.
use ProtossProbe.ProtossProbe as actionspec.
use ProtossZealot.ProtossZealot as actionspec.
use ProtossGateway.ProtossGateway as actionspec.
use ProtossForge.ProtossForge as actionspec.


test SimpleAgentProtoss with
	pre{ true }
	in{
		percept(self(Id,Type,_,_,_)) leadsto bel(self(Id,Type,_,_,_) ).
		bel(target(Type, Amount)) leadsto goal(target(Type, Amount)).
		
	}
	post{ true }
	
SimpleAgentProtoss {
	do main.
}