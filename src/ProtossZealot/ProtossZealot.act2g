use ProtossZealot as knowledge.

% attack action
define attack(ID) with
	pre { not(attack(_)) }
	post { true }
	
% move action
define move(X,Y) with 
	pre { not(move(_, _)) }
	post { true }

% attack at nearest coordinate action
define attack(X,Y) with
	pre { true }
	post { true }